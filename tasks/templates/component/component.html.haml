---
title: <:=
	// titleize
	name.split(/[\W_]/).map(function(word) {
		return word.charAt(0).toUpperCase() + word.substr(1);
	}).join(' ');
:>
---

- example 'Default' do

	= component '<:= name :>'
